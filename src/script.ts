function add(a: number, b: number): number {
    return a + b;
}
console.log(add(2, 6));

const a = ["a", "b"];
a.push("1")

let ab: number;
let ac: string;

let ee: string | number;

//array
let ad: string[] = [];
let dc: (number | string)[] = [];
